# ShellDataX
```
DBMS_Project/
│
├── dbms.sh                 # Main script (entry point)
├── databases/              # All databases stored here
│   ├── db1/
│   │   ├── table1.meta     # Table metadata (schema)
│   │   └── table1.data     # Table data (rows)
│   └── db2/
│
├── lib/
│   ├── main_menu.sh        # Main DB menu
│   ├── db_menu.sh          # Table menu (after connect)
│   ├── db_operations.sh    # Create, list, drop databases
│   ├── table_operations.sh # Create, list, drop tables
│   ├── record_ops.sh       # Insert, select, update, delete
│   └── validation.sh       # Datatype & PK validation
│
└── README.md
```
